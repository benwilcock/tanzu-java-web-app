spring:
  application:
    name: tanzu-java-web-app
  config:
    import: optional:configserver:http://spring-config-server.default.apps.example.com
  cloud:
    compatibility-verifier:
      enabled: true

configfrom: "YAML config (application.yml)"